# 🪑 웨이팅 관리 기획서 (waiting-management)

> 리뷰팡(핑크팽귄)에 붙이는 두 번째 기둥. 단독 기능이 아니라 **같은 고객 DB · 같은 쿠폰 지갑 ·
> 같은 데이터 원장을 공유**해서, 하나의 "올인원 장사도우미"로 합쳐지는 게 핵심.

---

## 1. 전략 — 웨이팅을 재방문 장치로 뒤집는다

웨이팅은 원래 **100% 부정적 경험**이다. 손님은 서서 기다리고, 사장님은 미안하다.
대부분의 웨이팅 앱은 여기서 끝난다 — "순서를 알려주는 것"까지만 한다.

리뷰팡의 웨이팅은 여기서 한 발 더 간다.

```
기다림(마이너스)  →  보상(플러스)  →  못 쓴 보상이 남음  →  다시 올 이유
```

**왜 이게 작동하나 (기존 쿠폰 정책과 맞물림)**
쿠폰은 하루 사용 한도가 있어서(현재 기본 2개) 받은 걸 그날 다 못 쓴다.
남은 쿠폰이 지갑에 쌓이고, 손님은 **아까워서라도 다시 온다.**
웨이팅 보상은 이 지갑에 넣는 또 하나의 입금 경로다.

> 즉 웨이팅은 "대기 관리 도구"가 아니라 **"대기 시간을 자산으로 바꾸는 장치"**다.

**부수 효과 — 연락처 자산**
웨이팅은 전화번호를 자연스럽게 받는다. 리뷰 게임을 안 해도 CRM DB가 쌓인다.
이건 퍼널의 "②-b 매장 자산(양도양수)" 논리와 그대로 이어진다.

---

## 2. 사용자 흐름

### 2-1. 손님
```
매장 앞 웨이팅 QR 스캔
   → 현재 대기 팀 수 · 예상 시간 확인
   → 이름/전화번호/인원 입력 후 등록
   → 내 순번 화면 (내 앞 N팀 · 예상 N분 · 경과 시간)
   → [사장님이 호출 문자] → 입장
   → 30분 넘게 기다렸다면 → 보상 자동 지급 (주사위 or 쿠폰)
```

### 2-2. 사장님
```
풀테이블 → 웨이팅 ON → QR 붙임
   → 관리자 '웨이팅' 탭에서 대기 목록 확인
   → 순서 된 팀 [호출] → 문자 앱 열림 → 전송
   → 손님 입장하면 [착석] → 목록에서 빠지고 뒷 순번이 한 칸씩 당겨짐
   → 안 오면 [노쇼]
```

---

## 3. 데이터 모델

```js
STORE.waiting = {
  enabled:false,          // 기능 사용 여부
  open:false,             // 지금 접수 받는 중인지 (풀테이블일 때 ON)
  avgMin:12,              // 팀당 평균 소요 시간(분) — 예상시간 계산용
  compensateAfterMin:30,  // 이 시간 초과 대기 시 보상
  compensateType:'dice',  // 'dice' | 'coupon' | 'none'
  compensateDice:1,
  compensateCoupon:'음료 1잔 무료',
  maxTeams:0,             // 0 = 무제한, N = N팀 넘으면 접수 마감
  list:[{
    id, name, phone, people,
    joinedAt,             // 등록 시각 (ms)
    status,               // 'waiting' | 'called' | 'seated' | 'cancelled' | 'noshow'
    calledAt, endedAt,
    waitedMin,            // 착석 시점에 계산해 저장
    compensated,          // 보상 지급 여부 (중복 지급 방지)
    uid                   // 기존 고객과 연결됐으면 그 id
  }]
}
```

**보관 정책**: `list`는 당일 것만 유지하고, 마감된 항목은 최근 200건까지만 남긴다.
(STORE는 JSON 블롭이라 무한히 쌓이면 용량 사고 — 사진 업로드 걷어낸 것과 같은 이유)
집계용 원본은 `store_events` 원장에 남기므로 블롭엔 최소만 둔다.

---

## 4. 화면

| 화면 id | 대상 | 내용 |
|---|---|---|
| `s-waiting` | 손님 | 현재 대기 현황 + 등록 폼 |
| `s-waiting-status` | 손님 | 내 순번 · 예상시간 · 경과시간 · 취소 |
| 관리자 `waiting` 탭 | 사장님 | 대기 목록 · 호출/착석/노쇼 · ON/OFF · 설정 · 전용 QR |

**진입 URL**: `?store=<매장ID>&w=1`
기존 `?store=` 파라미터 체계를 그대로 쓰고 `w=1`이면 웨이팅 화면으로 보낸다.
(게임 QR과 웨이팅 QR을 물리적으로 다른 종이에 붙일 수 있게 분리)

---

## 5. 핵심 규칙

**예상 시간** = `내 앞 대기 팀 수 × avgMin`
정확할 필요는 없지만 **실제보다 살짝 길게 보이는 게 낫다** — 예상보다 빨리 부르면 만족,
늦게 부르면 불만이라서. (초기값 팀당 12분)

**보상 지급 시점** = 사장님이 [착석] 누르는 순간
그때 `waitedMin`을 계산해 기준(기본 30분)을 넘었으면 자동 지급.
- `dice`: 해당 고객의 주사위 +N (기존 `diceState` 재사용)
- `coupon`: 쿠폰 발급 (기존 쿠폰 체계 재사용 → 일일 사용 한도도 그대로 적용됨)
- 중복 지급 방지를 위해 `compensated` 플래그로 1회만

**전화번호는 고객 DB에 합류**
웨이팅 등록 시 기존 `STORE.users`에 없는 번호면 새 고객으로 추가하고,
`store_customers` 테이블에도 upsert (연락처 유실 방지 구조 그대로 사용).
단 **마케팅 수신동의는 별도 체크박스**로 받는다 — 웨이팅 등록했다고 광고 동의는 아님(정보통신망법).

**호출 문자**는 기존 `sms:` 링크 방식 그대로 (발송 비용 0원).

---

## 6. 원장(store_events) 연동

이미 예약해둔 `waiting_join`을 포함해 3종을 남긴다.

| event | 시점 | meta |
|---|---|---|
| `waiting_join` | 손님 등록 | `{people, aheadTeams}` |
| `waiting_called` | 사장님 호출 | `{waitedMin}` |
| `waiting_done` | 착석/노쇼 | `{waitedMin, status, compensated}` |

→ `store_daily_funnel` 뷰에 이미 `waiting_joins` 컬럼이 있으므로
   QR조회 → 웨이팅 → 재방문까지 한 화면에서 전환율을 볼 수 있게 된다.

---

## 7. 구현 순서

1. **데이터 모델 + 마이그레이션** (`migrateStore`에 `waiting` 기본값)
2. **손님 화면** — 등록 + 내 순번
3. **사장님 관리 탭** — 목록/호출/착석/노쇼/ON·OFF
4. **보상 자동 지급** — 30분 초과 시 주사위·쿠폰
5. **전용 QR + 원장 연동**
6. 전체 검수 (라이트/다크 양쪽) 후 배포

---

## 8. 확인 필요 사항

- **쿠폰 일일 사용 한도**: 사장님은 "3개"로 기억하고 계시지만 현재 코드 기본값은 **2개**
  (`couponPolicy.dailyUseLimit`). 매장설정에서 바꿀 수 있는 값이라 그대로 두었는데,
  기본값을 3으로 올릴지 확인 필요.
