# 🪑 웨이팅 관리 기획서 (v2 — 개별 프로그램)

> **2026-08-11 방향 전환**: 리뷰팡 `index.html`에 직접 얹었던 첫 구현(웨이팅 화면·엔진 전체)은 롤백했다.
> 웨이팅 관리는 **리뷰팡과는 별도의 프로그램**으로 새로 만든다. 코드는 안 섞고,
> **데이터(고객 DB·연락처)와 보상 지급(주사위·쿠폰) 로직만 공유**하는 구조로 간다.
>
> 롤백 이유: 리뷰팡 단일 파일(`index.html`, 8천+ 줄)에 웨이팅 화면·관리 탭·엔진을 통째로
> 얹으면서 파일이 급격히 커지고, 리뷰 게임과 무관한 코드가 뒤섞여 유지보수가 어려워졌다.
> 웨이팅은 사용 패턴 자체가 다르다 — **태블릿 상시 화면**(직원이 접수) + **사장님 폰 화면**(호출)
> 조합이라, 리뷰팡의 "손님이 각자 폰으로 QR 찍고 들어오는" 모델과 UI 구조가 안 맞는다.

---

## 1. 왜 별도 프로그램인가

| | 리뷰팡 (기존) | 웨이팅 관리 (신규) |
|---|---|---|
| 진입 방식 | 손님 각자 폰으로 QR 스캔 | **매장 태블릿 1대**에 접수 화면 상시 표시 |
| 조작 주체 | 손님이 스스로 입력 | 직원이 태블릿에서 손님 대신 입력 (또는 손님이 직접) |
| 사장님 화면 | 관리자 대시보드(통계·CRM 등) 안의 탭 하나 | **독립된 웹앱** — 폰에서 바로 목록 보고 호출 |
| 갱신 빈도 | 게임 결과 확인 정도 | 초 단위로 계속 바뀌는 실시간 목록 (풀링/실시간 구독 필요) |

→ 화면 구조·갱신 방식이 완전히 다른 앱이라 **따로 만드는 게 맞다.**
공유할 건 코드가 아니라 **데이터**: 같은 Supabase 프로젝트, 같은 고객 원장, 같은 보상 파이프라인.

---

## 2. 전략은 그대로 — 웨이팅을 재방문 장치로 뒤집는다

```
기다림(마이너스)  →  보상(플러스)  →  못 쓴 보상이 남음  →  다시 올 이유
```

쿠폰 하루 사용 한도(이번에 **3개**로 상향, [index.html](index.html)) 덕분에 당첨된 걸 하루에 다 못 쓴다.
남은 쿠폰이 지갑에 쌓이고, 손님은 아까워서라도 다시 온다. 웨이팅 보상(주사위·쿠폰)은
이 지갑에 넣는 또 하나의 입금 경로 — 별도 프로그램이 되어도 이 로직 자체는 그대로 재사용한다.

**부수 효과 — 연락처 자산**: 웨이팅은 리뷰 게임 없이도 전화번호를 자연스럽게 받는다.
퍼널의 "매장 자산(양도양수)" 논리와 그대로 이어진다.

---

## 3. 사용자 흐름 (신규 구조)

### 3-1. 태블릿 (매장 카운터에 상시 거치)
```
사장님이 "웨이팅 ON" → 태블릿에 접수 화면 상시 표시
   → 손님이 이름/전화번호/인원 직접 입력 (또는 직원이 대신 입력)
   → 화면에 "현재 N팀 대기 중" 실시간으로 남음
```

### 3-2. 사장님 폰 (별도 웹앱, 로그인 후)
```
현재 대기 목록 확인 (실시간 갱신)
   → 순서 된 팀 [호출] → 문자 앱 열림 → 전송
   → 손님 입장하면 [착석] → 목록에서 빠지고 뒷 순번이 한 칸씩 당겨짐
   → 30분 이상 기다렸으면 자동으로 보상 대상 표시 → 착석 처리 시 주사위/쿠폰 지급
   → 안 오면 [노쇼]
```

### 3-3. 손님 (선택 사항)
자기 번호로 진행 상황을 보고 싶으면 문자에 링크를 넣어 "내 순번" 화면 제공 가능
(리뷰팡의 손님 화면 패턴 재사용 가능 — 이건 나중 단계).

---

## 4. 공유 vs 독립 — 경계선

**공유 (Supabase, 같은 프로젝트)**
- 고객 원장: 전화번호 기준으로 리뷰팡 `STORE.users`/`store_customers`와 매칭 — 웨이팅에서 처음 온 손님도
  리뷰팡 CRM 대상이 되고, 반대로 리뷰팡 기존 고객이 웨이팅에 등록하면 즉시 알아본다.
- 보상 파이프라인: 주사위 지급은 리뷰팡의 `diceState`, 쿠폰 지급은 리뷰팡의 `coupons` 배열에
  직접 쓴다 (같은 `stores.data` JSONB를 읽고 쓰되, **웨이팅 자체 데이터는 별도 테이블**로 분리 — 아래 참조).
- 매장 식별: 리뷰팡과 동일한 `store_id` 체계 사용.

**독립 (새 테이블/새 앱)**
- 웨이팅 대기열 자체: `waiting_queue` 신규 Supabase 테이블 (JSONB 블롭이 아니라 **행 단위 테이블**로 —
  실시간 갱신·정렬·필터가 잦아서 JSONB 블롭 방식은 안 맞음).
- 프론트엔드: 새 정적 페이지(태블릿용 1개 + 사장님용 1개), 리뷰팡 `index.html`과는 별도 파일.
- 실시간 갱신: Supabase Realtime 구독으로 폴링 없이 즉시 반영 (리뷰팡의 20초 폴링 방식보다 적합).

---

## 5. 데이터 모델 (Supabase 테이블 설계, 초안)

```sql
create table waiting_queue (
  id uuid primary key default gen_random_uuid(),
  store_id text not null,
  name text not null,
  phone text not null,
  people int not null default 1,
  status text not null default 'waiting',  -- waiting | called | seated | cancelled | noshow
  joined_at timestamptz not null default now(),
  called_at timestamptz,
  ended_at timestamptz,
  waited_min int,
  compensated boolean default false,
  uid text  -- 리뷰팡 STORE.users의 고객 id와 연결(매칭됐다면)
);

create table waiting_settings (
  store_id text primary key,
  open boolean default false,
  avg_min int default 12,
  compensate_after_min int default 30,
  compensate_type text default 'dice',   -- dice | coupon | none
  compensate_dice int default 1,
  compensate_coupon text default '음료 1잔 무료',
  max_teams int default 0
);
```

행 단위 테이블이라 Supabase Realtime으로 `waiting_queue` 변경을 그대로 구독 →
태블릿·사장님 폰 양쪽에 자동 반영. 마감된 항목은 주기적으로 정리(예: 7일 지난 것 삭제)하면
JSONB 블롭 용량 걱정 없이 무기한 운영 가능.

---

## 6. 핵심 규칙 (기존과 동일하게 유지)

- **예상 시간** = 내 앞 대기 팀 수 × `avg_min` — 실제보다 살짝 길게 보이는 게 낫다 (빨리 부르면 만족).
- **보상 지급 시점** = [착석] 누르는 순간. 기준 시간(기본 30분) 넘겼으면 자동 지급, `compensated`로 중복 방지.
- **전화번호는 고객 DB에 합류** — 신규 번호면 새 고객으로 추가, 마케팅 동의는 별도 체크박스로만.
- **호출 문자**는 `sms:` 링크 방식 (발송 비용 0원).
- 쿠폰 지급분은 리뷰팡의 하루 사용 한도(3개, 그중 메인 상품 1개)를 그대로 따른다 — 별도 시스템 취급 안 함.

---

## 7. 구현 순서

1. Supabase 테이블 생성(`waiting_queue`, `waiting_settings`) + RLS 정책
2. 태블릿용 접수 페이지 (단일 정적 HTML, 리뷰팡과 별도 파일/저장소 또는 같은 저장소의 새 디렉터리)
3. 사장님용 대기열 관리 페이지 (호출/착석/노쇼 + 실시간 구독)
4. 보상 지급 연동 — 리뷰팡 `stores.data`(diceState/coupons)에 직접 upsert
5. 고객 원장 매칭 — 전화번호로 `store_customers`/`STORE.users`와 연결
6. 라이트/다크 검수 후 배포

---

## 8. 확인 필요 사항

- 새 프로그램을 리뷰팡과 **같은 저장소**(예: `/waiting/` 하위 디렉터리)에 둘지,
  **완전히 별도 저장소·별도 Vercel 프로젝트**로 둘지 — 배포 흐름이 달라지니 결정 필요.
- 태블릿 접수 화면에 로그인이 필요한지(방치돼도 되는 공용 기기라 세션 유지 방식 고민 필요).
